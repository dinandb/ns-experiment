What we want to change to the results json file:
Add the modularity per dendrogram generated by algorithm each time it generates one
Add the time an algorithm takes each time it runs on a single graph, not the cummulative time it takes